---
name: daily-briefing
description: "giu의 일일 뉴스 브리핑을 작성할 때 사용. 국제 증시(SPY·QQQ·SCHD·SCHG와 편입 종목), 대륙별 국제 뉴스, 국내 뉴스, 국제개발협력(캄보디아 중심)을 한국어 웹 페이지로 정리한다."
---

# 일일 브리핑

매일 오전 8시(KST)에 자동 실행되며, 사용자가 직접 호출할 수도 있다. 산출물은 모두 한국어.

## 고정 아티팩트

링크 하나가 매일 갱신되는 방식이다. 새 아티팩트를 만들지 말 것.

```
https://claude.ai/code/artifact/68f1f50a-09ef-46cc-a0ad-464ae22ceeae
```

1. `date` 로 오늘 날짜와 직전 미국장 마감일을 확정한다.
2. 아래 4개 섹션 자료를 WebSearch / WebFetch 로 **먼저 전부** 수집한다.
3. `action: "read"` + 위 `url` 로 기존 페이지를 읽어 구조와 CSS를 그대로 가져온다.
4. 내용만 오늘자로 교체해 파일을 쓰고, 같은 `url` 로 재게시한다. 제목("오전 8시 브리핑")과 파비콘은 유지한다.

## 섹션 구성 (분량: 상세)

### 1. 국제 증시
- 지수: S&P 500 / 나스닥 / 다우 — 종가와 변동률
- 매크로: WTI·브렌트 유가, 미 10년물 금리, 달러지수(DXY), VIX
- ETF 4종: SPY, QQQ, SCHD, SCHG — 당일 움직임과 그날의 포인트
- 편입 종목 뉴스
  - SCHG / QQQ 쪽: NVDA, AAPL, MSFT, AMZN, GOOGL, AVGO, LLY, AMD, META
  - SCHD 쪽: MRK, AMGN, ABT, KO, VZ, CVX, HD, UNH, COP, PG
  - 당일 급등·급락 종목과 그 이유를 반드시 포함
- 표기: **상승은 적색, 하락은 청색** (국내 관행)

### 2. 국제 사회 — 대륙별
중동 / 유럽 / 아시아 / 아프리카 / 아메리카 / 오세아니아, 각 3~5개 항목.
항목마다 굵은 제목 한 구절 + 2~3문장 요약.

### 3. 국내 주요 뉴스
코스피·코스닥·원달러 환율 마감, 삼성전자·SK하이닉스 등 주요 종목, 정부 경기 판단과 경제 지표, 부처별 정책 발표.

### 4. 국제 개발협력
- **캄보디아를 중심 블록으로**: 거시 지표, 정부 정책, ADB·세계은행·IMF·UNDP 등 개발 파트너 동향, 봉제·관광·농업 산업 지표
- 배경: ODA 전반 동향(OECD DAC, 유엔, 주요 공여국 예산)과 한국 ODA(KOICA, EDCF)

### 추가 섹션
사용자가 그날 따로 요청한 주제가 있으면 섹션으로 덧붙인다.

## 마무리
출처 목록(제목 + 링크)을 반드시 페이지 하단에 넣는다.

## 날짜별 보관 (게시 직후, 매일)

고정 아티팩트는 매일 덮어써지므로 지난 판은 저장소에 남긴다.

- 저장소: `https://github.com/Gyu1718/newsforgyu`
- 사이트: `https://gyu1718.github.io/newsforgyu/` · 최신판 고정 주소 `latest.html`
- 원칙: `data/briefings.json`만 고친다. `docs/`는 GitHub Actions가 만드는 결과물이므로 직접 건드리지 않는다.

순서:

1. 저장소를 최신 상태로 당긴다(`git pull`, 처음이면 `git clone`).
2. 오늘 판 HTML을 임시 파일로 저장한 뒤 변환한다.
   ```bash
   python3 scripts/import_briefing_html.py <오늘판.html> YYYY-MM-DD
   ```
   같은 날짜 항목이 있으면 새 내용으로 교체된다. 변환 결과는 섹션·문단·출처 수로 확인한다.
3. 증시 섹션을 실은 날은 `figures`에 지수 등락률을 넣고, `market_date`를 직전 미국장 마감일로 바꾼다. 확인하지 못한 값은 `null`로 둔다.
4. 생성이 되는지 확인한다. `docs/` 변경분은 커밋하지 않는다.
   ```bash
   python3 scripts/build.py && python3 scripts/check_site.py
   git checkout -- docs
   ```
5. 데이터 파일만 커밋하고 푸시한다.
   ```bash
   git add data/briefings.json
   git commit -m "Add briefing YYYY-MM-DD"
   git push
   ```
6. 실행 로그 마지막 줄에 커밋 해시와 사이트 주소를 적는다. 푸시가 실패하면 변환된 JSON의 경로를 남기고 사용자에게 알린다.

오프라인 사본이 필요하면 같은 HTML을 `~/Documents/오전8시브리핑/YYYY/MM/YYYY-MM-DD.html`로도 저장한다. 저장소를 클론해 두면 `docs/briefings/`의 날짜별 페이지가 인터넷 없이도 열린다.

## 문체 규칙 (엄수)

- "~이 아닙니다", "A가 아니라 B입니다" 같은 부정 후 긍정 구조를 쓰지 않는다
- 두 개념을 세워 방향을 뒤집는 대조 구조를 피하고 "그러나 / 오히려 / 다만"을 쓰지 않는다
- "깊은", "바라보다", "자리", "~를 넘어(서)" 같은 상투어를 쓰지 않는다
- "현대 사회에서는", "결론적으로" 같은 에세이식 연결어를 쓰지 않는다
- "~해야 합니다" 식의 훈계조를 쓰지 않는다
- 주술 상응과 조사 사용을 정확히 한다
- 같은 문형의 반복을 피하고 문장 길이와 구조에 변주를 준다

## 사실 확인

확인되지 않은 수치는 추정해서 쓰지 않는다. 출처마다 수치가 다르면 그 차이를 짧게 밝힌다. 출처를 찾지 못한 값은 비워 두고 그 사실을 각주로 적는다.